import { ReactComponent as HeaderLogo } from '../../public/logo.svg'

import * as Styles from './Header.styles'

export default function Header () {
    return (
        <Styles.Logo>
                <HeaderLogo />
        </Styles.Logo>
    )
}